using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
091623

quick monobehavior implementation of background layer generator

*/

public class BackgroundLayer : MonoBehaviour
{
    ProbabilityWeightedSpritePool sprites;
    ProceduralLayoutParams layoutParams;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
